import { createStore } from "vuex";

export default createStore({
  state: {
    user: {},
    sidebar: {
      hasBackLink: true,
    },
    isTrialOver: false,
  },
  mutations: {},
  actions: {},
  modules: {},
});
